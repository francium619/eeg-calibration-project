# Python
__pycache__/
*.pyc

# Node (only needed to regenerate the .docx report; not source)
node_modules/

# Generated model artifacts -- regenerate via pretrain.py / meta_train.py / eval_calibration.py
*.npz
*.pt
*.pt.resume

# Run ledgers -- resumable state for long runs, not deliverables.
# Aggregated numbers belong in README.md.
results.jsonl
loso_results.jsonl

# Verification/scratch files from building this project (not deliverables)
page-*.jpg
lu52bof8.tmp
.~lock.*
